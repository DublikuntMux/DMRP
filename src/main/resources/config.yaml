# Player command output hear distance in blocks
hear_distance: 30
# Show update message on startup.
update: true

# Change to get success on try command
success_change: 50

# Max dices to throw in one shot
max_dices: 100
# Max sides in dice
max_sides: 255

# Max leash distance.
leash_distance: 15
